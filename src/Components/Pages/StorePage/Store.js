import React from "react";
import AvailableProducts from "../../Products/AvailableProducts";

const Store = () => {
    return (
        <div>
            <AvailableProducts />
        </div>
    );
};

export default Store;
